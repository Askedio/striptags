A super simple strip tags gem.


To use, download to libs/striptags then add to Gemfile

  gem 'striptags', :path => 'libs/striptags'

require it

  require 'striptags'

and use it

  "<b>test</b>".striptags

